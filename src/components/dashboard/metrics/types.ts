
import { LucideIcon } from 'lucide-react';

export interface MetricCardData {
  title: string;
  value: string | number;
  icon: LucideIcon;
  color: string;
  bgColor: string;
  borderColor: string;
}
